# Optional later: run Core (primary) on the Studio PC.
# Stage 1 only needs the home-server worker.

node:
  id: main-pc
  role: primary

worker_remote:
  url: "http://192.168.1.100:7850"   # <-- Heimserver LAN IP
  token: ""
